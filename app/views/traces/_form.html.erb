<%= form_for(@trace) do |f| %>
  <% if @trace.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trace.errors.count, "error") %> prohibited this trace from being saved:</h2>

      <ul>
      <% @trace.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_area :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
